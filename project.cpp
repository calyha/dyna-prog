#include <iostream>
using namespace std;

string direct("direct.txt");
string indirect("indirect.txt");

int main(){
	ifstream fin(direct);
	file.open(direct);

	if(fin.is_open()){
		cout << "Opened " << direct << "successfully!"\n;
		while(!fin.eof() && a < array_size && b < array_size){
			fin.get(array[a,b]);
			if(a = array_size - 1){
				b++;
				a = 0;
			}
			else{
				a++;
			}
		}

	}

	while(!file.eof()){
		string line;
		
	int n;
	int q;

	if (n % q != 0){
		throw Exception("q does not divide n");
	}

	double direct[n,n] = {2};
	double indirect[n,n] = {1};
return 1;
}